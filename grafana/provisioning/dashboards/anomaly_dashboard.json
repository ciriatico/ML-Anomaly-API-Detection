{
  "id": null,
  "title": "Anomaly Detection Metrics",
  "tags": ["anomaly", "api"],
  "timezone": "browser",
  "panels": [
    {
      "type": "graph",
      "title": "Training Requests",
      "targets": [{"expr": "train_requests_total", "legendFormat": "train requests"}],
      "datasource": "Prometheus",
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 6}
    },
    {
      "type": "graph",
      "title": "Prediction Requests",
      "targets": [{"expr": "predict_requests_total", "legendFormat": "predict requests"}],
      "datasource": "Prometheus",
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 6}
    },
    {
      "type": "graph",
      "title": "Training Duration (seconds)",
      "targets": [{"expr": "rate(train_duration_seconds_sum[1m]) / rate(train_duration_seconds_count[1m])", "legendFormat": "avg duration"}],
      "datasource": "Prometheus",
      "gridPos": {"x": 0, "y": 6, "w": 12, "h": 6}
    },
    {
      "type": "graph",
      "title": "Prediction Duration (seconds)",
      "targets": [{"expr": "rate(predict_duration_seconds_sum[1m]) / rate(predict_duration_seconds_count[1m])", "legendFormat": "avg duration"}],
      "datasource": "Prometheus",
      "gridPos": {"x": 12, "y": 6, "w": 12, "h": 6}
    }
  ],
  "schemaVersion": 36,
  "version": 1
}
